# =============================================================================
# Recipe Structurer V2 Configuration
# =============================================================================

# Choose ONE of the following providers:

# Option 1: OpenRouter (Recommended - one key for all models)
# -----------------------------------------------------------
OPENROUTER_API_KEY="your-openrouter-api-key-here"
# Get your key at: https://openrouter.ai/keys

# Option 2: DeepSeek Direct (slightly cheaper, direct connection)
# ---------------------------------------------------------------
# DEEPSEEK_API_KEY="your-deepseek-api-key-here"
# Get your key at: https://platform.deepseek.com/api_keys

# =============================================================================
# Nutrition Enrichment (Optional)
# =============================================================================
# USDA FoodData Central API key for nutritional data lookup
# Get your free key at: https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY="your-usda-api-key-here"

# =============================================================================
# Langfuse Observability (Optional)
# =============================================================================
# Self-hosted: docker compose up (see https://langfuse.com/self-hosting)
# LANGFUSE_PUBLIC_KEY="pk-lf-..."
# LANGFUSE_SECRET_KEY="sk-lf-..."
# LANGFUSE_HOST="http://localhost:3333"

# =============================================================================
# Private Access Secret
# =============================================================================
# Secret token required to access private recipes (crawled from paid sources).
# The client must send this in the X-Private-Token header.
# Generate a random string, e.g.: python -c "import secrets; print(secrets.token_urlsafe(32))"
PRIVATE_ACCESS_SECRET="change-me-to-a-random-secret"

# =============================================================================
# V1 Legacy Configuration (Deprecated)
# =============================================================================
# Only needed if using the old RecipeStructurer (V1)
# LLM_PROVIDER="deepseek"
# MISTRAL_API_KEY="your-mistral-api-key-here"
# HF_TOKEN="your-huggingface-token-here"
